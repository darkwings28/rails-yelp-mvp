<div class="container">
  <h1>Restaurants</h1>
    <%= link_to 'Add new ', new_restaurant_path, class: 'btn btn-sm btn-success' %>
  <hr>
   <% @restaurants.each do |restaurant| %>
     <p>
      <%= link_to restaurant.name, restaurant_path(restaurant), class: 'text-decoration-none' %>
   </p>
   <% end %>
</div>